# Comma-separated frontend origins. Never use * in production.
CORS_ORIGINS=http://localhost:5173

# Server-only secrets. Do not put these in VITE_ variables or the React app.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
